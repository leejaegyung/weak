# SE-HUB 보안 설계서

> 대상 독자: 개발자, 시스템 운영자

---

## 1. 인증 (Authentication)

### 1.1. JWT 토큰

| 항목 | 설정 |
|------|------|
| 유효 시간 | 8시간 |
| 알고리즘 | HS256 |
| 저장 위치 | HttpOnly Cookie (XSS 방지) |
| 갱신 | 만료 전 API 요청 시 자동 재발급 |

```php
// config/jwt.php
'ttl' => 480,         // 분 단위 (8시간)
'refresh_ttl' => 20160, // 14일 (리프레시 유효기간)
```

### 1.2. 비밀번호

- bcrypt (cost factor 12 이상) 해시 저장
- 평문 비밀번호는 어떤 경우에도 DB에 저장하지 않음
- 최소 8자 이상 검증 (FormRequest에서 처리)

---

## 2. 권한 (RBAC)

### 2.1. 역할 정의

| 역할 | 설명 |
|------|------|
| `admin` | 전체 일정·보고서 편집/삭제, 사용자 관리, SMTP 설정 |
| `member` | 본인 일정·보고서 작성/수정, 전체 보고서 열람 |

### 2.2. Laravel Policy 적용 기준

| 엔드포인트 | 권한 |
|------------|------|
| `PUT /schedules/{id}` | 본인 또는 admin |
| `POST /reports` | 본인 또는 admin |
| `DELETE /reports/{id}` | admin만 |
| `GET /reports` | 인증된 모든 사용자 |
| `POST /admin/*` | admin만 (Middleware로 일괄 처리) |

```php
// app/Http/Middleware/EnsureAdmin.php
if ($request->user()->role !== 'admin') {
    abort(403, '관리자 권한이 필요합니다.');
}
```

### 2.3. 비활성 계정 처리

`is_active = false` 계정은 JWT 발급 시점에 차단. 토큰이 이미 발급된 경우 미들웨어에서 추가 검증.

```php
if (!$user->is_active) {
    throw new AuthenticationException('비활성화된 계정입니다.');
}
```

---

## 3. API 보안

### 3.1. FormRequest 검증 강제

컨트롤러 내 인라인 검증 금지. 반드시 `app/Http/Requests/` 하위 클래스 사용.

### 3.2. CSRF 보호

Inertia.js는 자동으로 CSRF 토큰을 처리. API 엔드포인트는 `sanctum` 미들웨어 또는 JWT 인증으로 보호.

### 3.3. 대량 할당 방지

Eloquent 모델에 `$fillable` 명시적 선언 필수. `$guarded = []` 사용 금지.

```php
// 올바른 예시
protected $fillable = ['week', 'prev_support', 'curr_support', 'todo_items', 'internal_work', 'shared'];
```

---

## 4. 민감 정보 관리

### 4.1. SMTP 비밀번호

`settings` 테이블에 저장 시 Laravel `encrypt()` 사용. 읽기 시 `decrypt()` 복호화.

```php
// 저장
Setting::set('smtp_password', encrypt($request->smtp_password));

// 읽기
$password = decrypt(Setting::get('smtp_password'));
```

### 4.2. Google OAuth Refresh Token

`settings` 테이블에 동일하게 `encrypt()` 저장.

### 4.3. 환경 변수

DB 접속 정보, JWT 시크릿, APP_KEY는 `.env`에만 관리. `.env`는 `.gitignore`에 포함 필수.

---

## 5. Google OAuth2 (v2.0)

- 관리자 최초 1회 인증 → `access_token` + `refresh_token` DB 암호화 저장
- `access_token` 만료(1시간) 시 `refresh_token`으로 자동 갱신
- 권한 범위(scope): `https://www.googleapis.com/auth/drive.file` (최소 권한)

---

## 6. 보안 체크리스트

### 배포 전 확인

- [ ] `.env`의 `APP_KEY`가 생성되어 있는가? (`php artisan key:generate`)
- [ ] `APP_DEBUG=false`, `APP_ENV=production` 설정 확인
- [ ] DB 접속 계정이 최소 권한(SELECT/INSERT/UPDATE/DELETE만)인가?
- [ ] HTTPS 인증서 적용 및 HTTP → HTTPS 리다이렉트 설정 확인
- [ ] SMTP 비밀번호가 암호화 저장되는가?
- [ ] `.env`가 `.gitignore`에 포함되어 있는가?

### 정기 점검

- [ ] JWT 시크릿 키 주기적 교체 (분기 1회 권장)
- [ ] 관리자 계정 비밀번호 90일 이상 미변경 여부 확인
- [ ] 비활성화해야 할 퇴사/이동 계정 확인

### v2.0 추가 항목

- [ ] Google OAuth refresh token 유효 여부 확인
- [ ] Drive 파일 저장 폴더 접근 권한 점검
- [ ] OAuth scope가 `drive.file`로 최소화되어 있는가?

---

*문서 버전: v1.0 (SE-HUB 전용) | 최종 수정일: 2026-06-10*
