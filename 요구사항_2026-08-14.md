# 요구사항 및 이슈 목록

> **생성일**: 2026-08-14  
> **시스템**: 주간업무보고 시스템  
> **용도**: LLM 하네스 엔지니어링용 요구사항 관리 문서  

---

## 개요

| 상태 | 건수 |
|------|------|
| 완료 | 1 |
| **전체** | **1** |

---

## 완료

### #22 — 주간보고 미제출 인원 알람 대상 변경

| 항목 | 내용 |
|------|------|
| **작성자** | 김선호 |
| **상태** | 완료 |
| **등록일** | 2026-08-13 18:17 |
| **완료일** | 2026-08-14 |

**요구/이슈 내용:**

> 현재는 미제출 인원 알람에는 미제출 대상자만 지정되고 있습니다.  
> 작성 중인 인원들에 대해서도 미제출 대상자로 선정될 수 있도록 추가 요청 드립니다.  

**처리 내용:**

- 알림 대상 산정 로직을 `ReportService::notifyTargets()` 로 일원화 (기존에는 Webhook·카카오 컨트롤러에 중복 구현)
- 대상 = **해당 주차 보고서가 없는 인원(미제출) + 작성 중(draft) 인원**  
  제출됨·검토완료·반려됨은 제출한 것으로 보고 제외, 비활성·숨김 계정은 항상 제외
- 알림 메시지에 상태 구분 표기 — Webhook 은 `홍길동(작성 중)`, 카카오는 개인별 문구 분기
- 보고서 목록의 알림 모달에 발송 대상 명단·인원수 표시, 대상 0명이면 발송 버튼 비활성화

**변경 파일:**

| 파일 | 내용 |
|------|------|
| `app/Services/ReportService.php` | `notifyTargets()` 추가 |
| `app/Http/Controllers/SettingController.php` | 알림 대상 산정을 서비스로 위임, 상태별 메시지 분기 |
| `app/Http/Controllers/KakaoController.php` | 동일 (숨김 계정 제외 누락도 함께 해소) |
| `app/Http/Controllers/ReportController.php` | 목록 화면에 `alertTargets` 전달 |
| `resources/js/Pages/Report/Index.vue` | 알림 모달에 발송 대상 표시 |
| `resources/js/Pages/Admin/Webhook.vue` | 안내 문구 갱신, `channels` 누락으로 422 나던 버그 수정 |
| `resources/js/Pages/Admin/Kakao.vue` | 안내 문구 갱신 |

**비고:**

- **반려됨(rejected)** 은 현재 운영에서 사용하지 않는 상태이므로 알림 대상에서 제외 유지 (2026-08-14 확인)

---

