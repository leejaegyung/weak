# 🔐 보안 설계서 — 사내 원격 데스크탑 프로그램 (IHRD)
**대상 독자: 보안 담당자, 정보보호 책임자**

---

## 목차

1. [보안 개요](#1-보안-개요)
2. [위협 모델 (Threat Model)](#2-위협-모델)
3. [인증 및 접근 제어](#3-인증-및-접근-제어)
4. [전송 보안](#4-전송-보안)
5. [운영 보안](#5-운영-보안)
6. [감사 로그](#6-감사-로그)
7. [취약점 및 한계](#7-취약점-및-한계)
8. [보안 체크리스트](#8-보안-체크리스트)

---

## 1. 보안 개요

IHRD는 **사내 LAN 전용** 원격 제어 솔루션으로, 외부 인터넷 경유 없이 내부 네트워크 내에서만 동작한다. 상용 솔루션 대비 다음의 보안 이점을 가진다.

| 항목 | 상용 솔루션 (TeamViewer 등) | IHRD |
|------|---------------------------|------|
| 데이터 경로 | 외부 중계 서버 경유 | 사내 네트워크 직접 P2P |
| 화면 데이터 저장 | 중계 서버 메모리 통과 | 저장 없음 |
| 라이선스 관리 | 외부 업체 의존 | 자체 관리 |
| 접근 제어 | 계정 기반 | IP + 사용자 동의 |

---

## 2. 위협 모델

### 2.1 공격 대상 (Assets)

| 자산 | 설명 | 민감도 |
|------|------|--------|
| 화면 데이터 | 실시간 전송되는 대상 PC 화면 | 🔴 높음 |
| 입력 데이터 | 마우스/키보드 이벤트 (패스워드 포함 가능) | 🔴 높음 |
| 접속 로그 | 누가 언제 어디에 접속했는지 | 🟡 중간 |
| 설정 파일 | IP 대역, 포트, 보안 설정 | 🟡 중간 |

### 2.2 위협 시나리오 (STRIDE)

| 위협 유형 | 시나리오 | 대응 방안 |
|-----------|---------|-----------|
| **S**poofing (위장) | 공격자가 IT 담당자인 척 접속 | IP 화이트리스트 + 허용 팝업 |
| **T**ampering (변조) | 전송 중 입력 이벤트 패킷 변조 | 패킷 Magic bytes 검증 (v1), TLS (v2) |
| **R**epudiation (부인) | "나는 접속 안 했다" 주장 | 감사 로그 (IP, 시각, 결과) |
| **I**nformation Disclosure (노출) | 네트워크 스니핑으로 화면 탈취 | TLS 암호화 권장 (v2) |
| **D**enial of Service (서비스 거부) | 반복 접속 요청으로 Agent 과부하 | 연결 속도 제한, 동시 1세션 |
| **E**levation of Privilege (권한 상승) | Agent 취약점으로 시스템 권한 획득 | 최소 권한 원칙, UAC 적용 |

---

## 3. 인증 및 접근 제어

### 3.1 Layer 1 — IP 화이트리스트

```ini
# config.ini
[security]
allowed_ip_range = 192.168.1.0/24
```

- 허용 IP 대역 외 연결 요청은 **즉시 소켓 차단** (팝업 없음)
- 로그에 `BLOCKED (IP_NOT_ALLOWED)` 기록
- 복수 대역 지원 필요 시: 쉼표 구분 확장 가능

```python
import ipaddress

def is_allowed(ip: str, allowed_range: str) -> bool:
    return ipaddress.ip_address(ip) in ipaddress.ip_network(allowed_range)
```

### 3.2 Layer 2 — 사용자 동의 팝업

```
┌─────────────────────────────────┐
│  🖥️ 원격 접속 요청               │
├─────────────────────────────────┤
│  192.168.1.42 에서               │
│  원격 접속을 요청했습니다.         │
│                                 │
│  허용하시겠습니까?                │
│  (30초 내 응답 없으면 자동 거부)  │
├─────────────────────────────────┤
│        [거부]     [허용]         │
└─────────────────────────────────┘
```

- IP 화이트리스트 통과 후 **반드시** 사용자 확인 필요
- `require_approval = false` 로 비활성화 가능 (비권장)
- 30초 내 무응답 시 **자동 거부** 처리

### 3.3 Layer 3 — 세션 비밀번호 (선택, v1.1+)

```
Host에서 1회용 6자리 PIN 생성 → 사용자에게 구두/메신저로 전달
Viewer에서 연결 시 PIN 입력 → 불일치 시 연결 거부
PIN은 1회 사용 후 폐기, 세션 시작과 동시에 만료
```

### 3.4 세션 자동 종료

```ini
[security]
session_timeout = 3600  # 1시간 후 자동 종료 (초)
```

---

## 4. 전송 보안

### 4.1 v1 — 평문 전송 (사내 LAN 한정)

v1은 사내 LAN 환경에서만 동작한다는 전제 하에 평문 TCP 전송을 사용한다.

**전제 조건**
- 유선 LAN 또는 WPA3 Wi-Fi 환경
- 사내 네트워크 분리 (외부 접근 불가)
- 스위치 레벨에서 VLAN 격리

> ⚠️ Wi-Fi 환경에서는 네트워크 스니핑 위험이 있으므로 TLS 적용을 강력히 권장

### 4.2 v2 — TLS 암호화 적용 (권장)

```python
import ssl

# Server (Host Agent)
context = ssl.SSLContext(ssl.PROTOCOL_TLS_SERVER)
context.load_cert_chain(certfile="server.crt", keyfile="server.key")
secure_conn = context.wrap_socket(raw_conn, server_side=True)

# Client (Viewer)
context = ssl.SSLContext(ssl.PROTOCOL_TLS_CLIENT)
context.load_verify_locations("ca.crt")
secure_conn = context.wrap_socket(raw_conn, server_hostname=host_ip)
```

**인증서 관리**
- 사내 CA(Certificate Authority) 구축 후 내부 인증서 발급
- 또는 각 Host PC에 자체 서명 인증서 배포
- 인증서 유효 기간: 1년 권장, 만료 30일 전 갱신 알림

### 4.3 전송 데이터 보안 정책

| 데이터 | 전송 방식 | 디스크 저장 |
|--------|-----------|------------|
| 화면 프레임 | 메모리 → 소켓 | ❌ 없음 |
| 마우스/키보드 이벤트 | 메모리 → 소켓 | ❌ 없음 |
| 연결 로그 | 로컬 파일 기록 | ✅ (IP/시각만) |
| 설정 정보 | config.ini | ✅ (암호화 불필요) |

---

## 5. 운영 보안

### 5.1 최소 권한 원칙

- Host Agent는 일반 사용자 권한으로 실행 가능
- UAC가 걸린 앱 제어 필요 시에만 관리자 권한 요청
- 서비스(Service) 등록 시 `LocalService` 계정 사용 권장

### 5.2 Host Agent 실행 파일 무결성

```powershell
# 배포 전 해시값 생성 (배포자)
Get-FileHash IHRD-Host.exe -Algorithm SHA256

# 설치 후 검증 (설치자)
Get-FileHash IHRD-Host.exe -Algorithm SHA256
# → 두 값 일치 확인
```

### 5.3 설정 파일 접근 제어

```
config.ini 파일은 Host Agent 실행 디렉토리에 위치
ACL 설정: 관리자 그룹만 수정 가능, 일반 사용자는 읽기 전용
```

### 5.4 비인가 접근 대응 절차

```
1. 로그에서 BLOCKED 또는 DENIED 이벤트 다수 발생 확인
2. 출발지 IP 확인 → 사내 IP 대역 내 비정상 IP 여부 파악
3. 해당 PC 격리 및 감염 여부 점검
4. 필요 시 allowed_ip_range 범위 축소 조정
```

---

## 6. 감사 로그

### 6.1 로그 항목 정의

| 이벤트 | 로그 레벨 | 기록 항목 |
|--------|-----------|-----------|
| 접속 허용 | INFO | 시각, 출발지 IP, 대상 IP |
| 접속 거부 (사용자) | WARNING | 시각, 출발지 IP, 거부 주체 |
| 접속 차단 (IP) | WARNING | 시각, 출발지 IP, 차단 사유 |
| 세션 종료 | INFO | 시각, 출발지 IP, 세션 지속 시간 |
| 오류 발생 | ERROR | 시각, 오류 유형, 스택 트레이스 |

### 6.2 로그 예시

```
2026-04-15 09:32:11 | INFO    | CONNECT    | src=192.168.1.42 dst=192.168.1.101 result=ALLOWED
2026-04-15 09:45:03 | INFO    | DISCONNECT | src=192.168.1.42 dst=192.168.1.101 duration=772s
2026-04-15 10:01:55 | WARNING | CONNECT    | src=192.168.1.88 dst=192.168.1.101 result=DENIED(user)
2026-04-15 10:15:20 | WARNING | BLOCKED    | src=10.0.0.55   dst=192.168.1.101 reason=IP_NOT_ALLOWED
2026-04-15 11:00:00 | WARNING | BLOCKED    | src=10.0.0.55   dst=192.168.1.101 reason=IP_NOT_ALLOWED
2026-04-15 11:00:01 | WARNING | BLOCKED    | src=10.0.0.55   dst=192.168.1.101 reason=IP_NOT_ALLOWED
```

### 6.3 로그 보존 정책

| 항목 | 설정 |
|------|------|
| 보존 기간 | 최소 90일 |
| 파일 형식 | `ihrd_YYYYMMDD.log` (일별 파일) |
| 저장 경로 | `C:\IHRD\logs\` |
| 로그 접근 권한 | 관리자 그룹만 읽기 가능 |
| 로그 변조 방지 | append-only 모드, 정기 백업 권장 |

---

## 7. 취약점 및 한계

### 7.1 알려진 한계 (v1)

| # | 한계 | 위험도 | 해결 계획 |
|---|------|--------|-----------|
| L1 | 평문 전송 (TLS 미적용) | 🟡 중간 | v2에서 TLS 적용 |
| L2 | 세션 비밀번호 미지원 | 🟡 중간 | v1.1에서 PIN 추가 |
| L3 | 인증서 기반 상호 인증 없음 | 🟡 중간 | v2에서 mTLS 고려 |
| L4 | 동시 1세션 제한 (DoS 부분 완화만) | 🟢 낮음 | 속도 제한 추가 예정 |

### 7.2 수용 가능한 위험 (사내 LAN 환경 전제)

- 평문 전송: 사내 유선 LAN은 스니핑 위험이 낮음
- IP 스푸핑: 사내 스위치 환경에서 ARP 스푸핑 가능성 낮음

> ⚠️ Wi-Fi 환경이나 VPN을 통한 외부 접속 시나리오에서는 TLS 적용 전까지 사용 금지

---

## 8. 보안 체크리스트

### 배포 전 확인

- [ ] `allowed_ip_range`가 최소 필요 범위로 설정되었는가?
- [ ] `require_approval = true`로 설정되었는가?
- [ ] `session_timeout`이 적절히 설정되었는가?
- [ ] Host PC 방화벽에서 포트 9000만 열려 있는가?
- [ ] config.ini 파일 접근 권한이 관리자 전용인가?
- [ ] IHRD-Host.exe 해시값이 배포 원본과 일치하는가?

### 정기 운영 점검 (월 1회)

- [ ] 로그에서 비정상 접속 시도(BLOCKED/DENIED) 다수 여부 확인
- [ ] 로그 파일이 정상적으로 생성 및 보존되고 있는가?
- [ ] allowed_ip_range가 현재 조직 구조에 맞게 유효한가?
- [ ] 퇴사/이동한 직원의 PC가 Viewer로 사용될 가능성은 없는가?

### v2 업그레이드 시 추가 항목

- [ ] TLS 인증서 유효 기간 확인
- [ ] CA 인증서가 모든 Host PC에 배포되었는가?
- [ ] mTLS(상호 인증) 적용 여부 검토

---

*문서 버전: v1.0 | 작성일: 2026-04-15 | 역할: 보안*
