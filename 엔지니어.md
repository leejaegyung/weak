# SE-HUB 엔지니어링 설계서

> 아키텍처, 데이터베이스, 인프라 설계 문서. 코딩 표준은 `개발.md` 참조.

---

## 1. 프로젝트 개요

| 항목 | 내용 |
| :--- | :--- |
| **프로젝트명** | 주간업무보고 시스템 (SE-HUB) |
| **플랫폼** | 웹 (Nginx + Laravel 11.x + Vue 3) |
| **사용 환경** | 사내 LAN / 브라우저 |
| **데이터베이스** | PostgreSQL 17 |
| **인증 방식** | JWT (8시간) + bcrypt 암호화 |

---

## 2. 아키텍처

### 2.1. 레이어 구조

```
Nginx (포트 80/443)
  └─ Laravel (PHP-FPM)
       ├─ Routes (web.php / api.php)
       ├─ Controllers  ← 얇게 유지 (요청 수신·검증·응답만)
       ├─ Services (app/Services/)  ← 비즈니스 로직·DB 쿼리 전담
       ├─ Models (Eloquent ORM)
       └─ Inertia → Vue 3 SPA
```

**Thin Controller 원칙:** 컨트롤러는 FormRequest 검증 후 Service 호출, Inertia 응답 반환만 수행.

```php
// 올바른 예시
public function store(StoreReportRequest $request, ReportService $service)
{
    $report = $service->createOrUpdate($request->validated(), auth()->user());
    return redirect()->route('reports.show', $report);
}
```

### 2.2. 주요 서비스 클래스

| 클래스 | 책임 |
|--------|------|
| `ReportService` | 보고서 생성/수정/조회, 미작성자 포함 목록 조회 |
| `ScheduleService` | 주차 전환 로직, 일정 그리드 데이터 조합 |
| `UserService` | 계정 생성·수정·비활성화 |
| `MailService` | SMTP 설정 로드, 주간보고 메일 발송 |
| `DriveService` | Google Sheets API 연동, Drive 파일 저장 (v2.0) |

---

## 3. 데이터베이스 스키마 (PostgreSQL)

### users

| 컬럼 | 타입 | 설명 |
|------|------|------|
| `id` | BIGSERIAL PK | |
| `username` | VARCHAR(64) UNIQUE | 로그인 ID |
| `password_hash` | VARCHAR(255) | bcrypt |
| `name` | VARCHAR(64) | 표시 이름 |
| `position` | VARCHAR(32) | 직급 (사원/대리/과장/팀장 등) |
| `role` | VARCHAR(16) | `admin` 또는 `member` |
| `is_active` | BOOLEAN | 비활성 계정 로그인 차단 |
| `sort_order` | INTEGER | 일정판·목록 표출 순서 |

### reports

| 컬럼 | 타입 | 설명 |
|------|------|------|
| `id` | BIGSERIAL PK | |
| `user_id` | BIGINT FK → users | |
| `week` | VARCHAR(8) | 예: `2026-W17` |
| `prev_support` | JSONB | 전주 지원 `[{title, content}]` |
| `curr_support` | JSONB | 금주 지원 `[{title, content}]` |
| `todo_items` | JSONB | `[{content}]` |
| `internal_work` | TEXT | 개행문자 보존 |
| `shared` | TEXT | 개행문자 보존 |

> `prev_support`와 `curr_support`는 완전히 독립된 배열. 개수·순서 일치 불필요.

### schedules

| 컬럼 | 타입 | 설명 |
|------|------|------|
| `id` | BIGSERIAL PK | |
| `user_id` | BIGINT FK → users | |
| `date` | DATE | `YYYY-MM-DD` |
| `content` | TEXT | 해당 날짜 업무 내용 |

### settings (v2.1+)

| 컬럼 | 타입 | 설명 |
|------|------|------|
| `key` | VARCHAR(64) PK | 설정 키 |
| `value` | TEXT | 설정 값 (SMTP 비밀번호는 암호화 저장) |

---

## 4. 핵심 비즈니스 로직

### 4.1. 주차 자동 전환

서버 타임 기준 `target_date`를 계산하여 금주/차주를 동적으로 응답. 주차 전환 시 차주 내용이 금주로 이동.

```php
// ScheduleService
public function getCurrentAndNextWeek(): array
{
    $now = now();
    $currentWeekStart = $now->startOfWeek(Carbon::MONDAY);
    $nextWeekStart = $currentWeekStart->copy()->addWeek();
    return [$currentWeekStart, $nextWeekStart];
}
```

### 4.2. 보고서 목록 — 미작성자 포함

`LEFT JOIN`으로 보고서 미작성 유저도 `sort_order` 순으로 전체 노출.

```php
// ReportService
public function listByWeek(string $week): Collection
{
    return User::where('is_active', true)
        ->orderBy('sort_order')
        ->leftJoin('reports', function ($join) use ($week) {
            $join->on('users.id', '=', 'reports.user_id')
                 ->where('reports.week', $week);
        })
        ->select('users.*', 'reports.id as report_id', ...)
        ->get();
}
```

### 4.3. 엔터키 처리

텍스트 필드 내 엔터는 개행(`\n`)으로만 동작. 폼 제출은 버튼 클릭으로만 가능.

```vue
<!-- Vue 컴포넌트 -->
<textarea @keydown.enter.prevent="handleEnter" />
```

---

## 5. 인프라

### 5.1. Nginx 설정 요약

```nginx
server {
    listen 80;
    root /var/www/sehub/public;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php8.3-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
    }
}
```

### 5.2. Google Drive 연동 (v2.0)

- OAuth2 인증: 관리자 최초 1회 인증 → refresh token DB 저장, 이후 자동 갱신
- 파일명 규칙: `YYYY.MM.DD SE팀 주간보고 (이름 직급).xlsx`
- API 흐름: 보고서 저장 → `POST /api/drive/export/{id}` → Sheets API 시트 생성 → 지정 폴더 저장 → 링크 반환

### 5.3. 메일 전송 (v2.1)

- SMTP 설정은 `settings` 테이블 관리, 비밀번호는 `encrypt()` 저장
- 전원 제출 완료 확인 후에만 전송 버튼 활성화
- `MailService::sendWeeklyReport(week, reportLinks[])` → Laravel Mail + SMTP 설정값 사용

---

*문서 버전: v2.1 (Laravel 기준) | 최종 수정일: 2026-06-10*
