# SE-HUB 개발자 가이드 (Developer Guide)

이 문서는 SE-HUB 프로젝트의 코드 일관성, 유지보수성, 그리고 높은 소프트웨어 품질을 유지하기 위한 개발 표준 가이드입니다. 모든 개발자와 AI 어시스턴트는 코드를 작성할 때 이 가이드를 최우선으로 준수해야 합니다.

---

## 1. 🤖 AI 행동 및 기본 원칙
* **문서 기반 개발:** 코드를 작성하기 전 반드시 `PRD.md` 및 `TECH_SPEC.md`를 검토하여 비즈니스 요구사항을 파악합니다.
* **최소 수정 원칙:** 요청받은 기능과 관련 없는 기존 로직이나 파일을 임의로 수정하거나 삭제하지 않습니다.
* **언어 규칙:** 코드 레벨의 네이밍을 제외한 모든 UI 텍스트(라벨, 메시지, 주석 등)는 반드시 **한국어**로 작성합니다.

---

## 2. 🎨 프론트엔드 (Vue 3 + Inertia + Tailwind)

### 2.1. Vue.js 컴포넌트
* **Composition API 사용:** 모든 컴포넌트는 `<script setup>` 구문을 사용합니다. Options API 사용은 금지됩니다.
* **상태 관리 및 폼 처리:** 폼 전송, 데이터 바인딩, 유효성 에러 처리는 반드시 `@inertiajs/vue3`의 `useForm` 훅을 사용합니다.
* **컴포넌트 분리:** 코드 150줄 이상 또는 반복되는 UI(버튼, 모달 등)는 `resources/js/Components/` 하위로 분리하여 재사용합니다.

### 2.2. 스타일링 (Tailwind CSS)
* **Utility-First:** 모든 스타일은 Tailwind CSS 유틸리티 클래스로 작성하며, `<style>` 태그 내 커스텀 CSS 작성은 금지합니다.
* **디자인 지향점:** B2B 엔터프라이즈 시스템에 적합한 충분한 여백(`p-6`, `gap-4`)과 부드러운 모서리(`rounded-md`)를 적용합니다.

---

## 3. ⚙️ 백엔드 (Laravel 11.x)

### 3.1. 아키텍처 및 로직 분리
* **Thin Controller:** 컨트롤러는 요청 수신, 검증, 서비스 호출, 응답 반환 역할만 수행합니다.
* **Fat Service:** 모든 비즈니스 로직과 DB 쿼리는 `app/Services` 폴더 내의 서비스 클래스에서 처리합니다.
* **Inertia 응답:** 데이터 렌더링 시 `Inertia::render('경로', [데이터])` 형식을 사용합니다.

### 3.2. 데이터 검증 및 보안
* **FormRequest:** 컨트롤러 내 하드코딩된 검증을 금지하며, 반드시 `app/Http/Requests` 하위의 독립된 Form Request 클래스를 사용합니다.
* **권한 검증:** 수정/삭제 엔드포인트는 반드시 Laravel Policy 또는 Middleware를 통해 권한(본인 확인 등)을 검토해야 합니다.

---

## 4. 📝 네이밍 컨벤션 (Naming Conventions)

| 대상 | 컨벤션 | 예시 |
| :--- | :--- | :--- |
| **Database** | `snake_case` | `weekly_reports`, `start_date` |
| **PHP Class / Model** | `PascalCase` | `WeeklyReport`, `ScheduleController` |
| **PHP Variable / Method** | `camelCase` | `$weeklyReport`, `getSchedules()` |
| **JS / TS Variable** | `camelCase` | `handleFormSubmit`, `fetchData` |
| **Vue File** | `PascalCase.vue` | `DashboardView.vue`, `StatusBadge.vue` |
| **URL Route** | `kebab-case` | `/weekly-reports/create` |