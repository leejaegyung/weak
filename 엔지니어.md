# ⚙️ 엔지니어링 설계서 — 사내 원격 데스크탑 프로그램 (IHRD)
**대상 독자: 엔지니어, 백엔드/인프라 담당자**

---

## 목차

1. [시스템 아키텍처](#1-시스템-아키텍처)
2. [네트워크 설계](#2-네트워크-설계)
3. [스레드 모델](#3-스레드-모델)
4. [성능 최적화 전략](#4-성능-최적화-전략)
5. [에러 처리 및 복구](#5-에러-처리-및-복구)
6. [설정 파일 명세](#6-설정-파일-명세)
7. [로깅 설계](#7-로깅-설계)
8. [배포 및 운영 절차](#8-배포-및-운영-절차)

---

## 1. 시스템 아키텍처

### 1.1 전체 구성도

```
┌─────────────────────────────────────────────────────────────┐
│                      사내 네트워크 (LAN)                      │
│                                                             │
│   ┌──────────────────┐         ┌──────────────────────┐    │
│   │   Viewer PC      │         │   Host PC (대상)      │    │
│   │                  │◄───────►│                       │    │
│   │  [ Viewer App ]  │ TCP     │  [ Host Agent ]       │    │
│   │  - 화면 렌더링    │ 9000   │  - 화면 캡처           │    │
│   │  - 입력 전송     │         │  - 입력 실행           │    │
│   │  - UI 컨트롤     │         │  - 접속 허용 팝업      │    │
│   └──────────────────┘         └──────────────────────┘    │
│                                                             │
│              ※ P2P 직접 연결 — 중계 서버 없음                │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 데이터 흐름

```
[Host Agent]                              [Viewer App]
    │                                          │
    │  1. TCP Listen on :9000                  │
    │◄─────────── 2. connect() ───────────────│
    │                                          │
    │  3. 허용 팝업 → 사용자 응답               │
    │──────────── 4. control/approved ────────►│
    │                                          │
    │  ┌─ Capture Thread                       │
    │  │  mss.grab() → Pillow JPEG 압축        │
    │  │  → pack() → send()                   │
    │──┼──────────── frame stream ────────────►│
    │  └─ (20 FPS loop)                        │  ┌─ Receive Thread
    │                                          │  │  recv() → unpack()
    │◄──────────── mouse/keyboard ────────────│◄─┘  → UI signal emit
    │                                          │
    │  Input Thread                            │
    │  unpack() → InputHandler.handle()        │
    │  → pyautogui 실행                        │
```

---

## 2. 네트워크 설계

### 2.1 포트 및 프로토콜

| 항목 | 값 |
|------|-----|
| 프로토콜 | TCP |
| 기본 포트 | 9000 |
| 방향 | Host가 Listen, Viewer가 Connect |
| 최대 연결 수 (v1) | 1 (동시 세션 1개) |

### 2.2 패킷 구조

```
┌──────────┬──────────┬──────────────────────────────┐
│  Magic   │  Length  │         Payload (JSON)        │
│  4 bytes │  4 bytes │         (variable)            │
└──────────┴──────────┴──────────────────────────────┘

Magic  : b'IHRD' — 유효하지 않은 연결 필터링용
Length : Big Endian uint32 — Payload 바이트 길이
```

### 2.3 수신 로직 (Framing)

TCP는 스트림 프로토콜이므로 반드시 정확한 바이트 수를 읽어야 한다.

```python
def recv_exact(sock, n):
    buf = b''
    while len(buf) < n:
        chunk = sock.recv(n - len(buf))
        if not chunk:
            raise ConnectionError("연결 끊김")
        buf += chunk
    return buf

def recv_message(sock):
    header = recv_exact(sock, 8)
    magic = header[:4]
    if magic != b'IHRD':
        raise ValueError("잘못된 Magic bytes")
    length = int.from_bytes(header[4:8], 'big')
    payload = recv_exact(sock, length)
    return json.loads(payload)
```

### 2.4 방화벽 설정 (Host PC)

```powershell
# 인바운드 TCP 9000 허용 (관리자 권한)
netsh advfirewall firewall add rule `
  name="IHRD Host Agent" `
  dir=in action=allow `
  protocol=TCP localport=9000
```

---

## 3. 스레드 모델

### 3.1 Host Agent 스레드

```
[Host Agent 프로세스]
│
├── Main Thread
│   └── tkinter 이벤트 루프 (트레이 아이콘, 허용 팝업)
│
├── Server Thread
│   └── socket.accept() 루프 → 연결 수락 시 핸들러 스레드 생성
│
├── Capture Thread  (세션 연결 후 생성)
│   └── 캡처 → 압축 → 전송 루프 (20 FPS)
│
└── Input Thread  (세션 연결 후 생성)
    └── 입력 이벤트 수신 → InputHandler 호출
```

### 3.2 Viewer App 스레드

```
[Viewer App 프로세스]
│
├── Main Thread
│   └── PyQt6 이벤트 루프 (UI 렌더링, 마우스/키보드 캡처)
│
├── Receive Thread  (연결 후 생성)
│   └── 프레임 수신 → Queue에 적재 → UI 시그널 emit
│
└── Send Thread  (연결 후 생성)
    └── 입력 이벤트 Queue 소비 → 전송
```

### 3.3 스레드 안전성

- PyQt6 UI 업데이트는 **반드시 Main Thread**에서 수행
- 수신 스레드 → `pyqtSignal` → Main Thread 렌더링 (Qt 시그널/슬롯 사용)
- `Queue.Queue`로 프레임 버퍼 관리 (thread-safe)

---

## 4. 성능 최적화 전략

### 4.1 화면 캡처 최적화

```
해상도 다운스케일  : 1920×1080 → 1280×720 (필요 시)
                   Pillow.resize(LANCZOS)

JPEG 품질 조절    : 기본 quality=60
                   네트워크 지연 증가 시 자동으로 40까지 낮춤 (dynamic quality)

캡처 라이브러리   : mss (GDI Direct) — PIL.ImageGrab 대비 2~3배 빠름
```

### 4.2 네트워크 Back-pressure

```
송신 측 : 이전 프레임 전송 완료 후 다음 캡처 시작 (blocking send)
수신 측 : Queue(maxsize=3) — 큐 가득 차면 가장 오래된 프레임 드롭
          → 네트워크 지연이 있어도 최신 화면 유지
```

### 4.3 FPS 제어

```python
TARGET_FPS = 20
FRAME_INTERVAL = 1.0 / TARGET_FPS  # 0.05초

while running:
    t_start = time.monotonic()
    frame = capture.capture_frame(quality=60)
    send_message(conn, {"type": "frame", "data": b64encode(frame)})
    elapsed = time.monotonic() - t_start
    sleep_time = max(0, FRAME_INTERVAL - elapsed)
    time.sleep(sleep_time)
```

### 4.4 성능 목표 (사내 LAN 1Gbps 기준)

| 항목 | 목표 | 예상 달성 |
|------|------|----------|
| 프레임 전송 FPS | 20 FPS | ✅ 가능 |
| 입력 이벤트 지연 | ≤ 50ms | ✅ 가능 |
| JPEG 프레임 크기 | ~30~80KB | quality=60 기준 |
| 평균 네트워크 사용량 | ~15~25 Mbps | ✅ LAN 충분 |

---

## 5. 에러 처리 및 복구

### 5.1 에러 상황별 처리

| 상황 | 감지 방법 | 처리 방법 |
|------|-----------|-----------|
| 네트워크 끊김 | `ConnectionError` / `recv()` 빈 bytes | 자동 재연결 3회 시도 |
| 대상 PC 거부 | `control/denied` 수신 | Viewer에 "접속 거부됨" 알림 |
| 포트 충돌 | `OSError: [WinError 10048]` | config.ini에서 포트 변경 안내 |
| 화면 캡처 실패 | `mss.exception` | 예외 캐치 후 해당 프레임 스킵 |
| 입력 실행 실패 | `pyautogui.FailSafeException` | 로그 기록 후 세션 유지 |
| 연결 타임아웃 | `socket.timeout` | 연결 해제 및 재시도 안내 |

### 5.2 재연결 로직

```python
MAX_RETRY = 3
RETRY_DELAY = 3  # 초

for attempt in range(MAX_RETRY):
    try:
        client.connect(host_ip, port)
        break
    except ConnectionRefusedError:
        if attempt < MAX_RETRY - 1:
            time.sleep(RETRY_DELAY)
        else:
            show_error("연결 실패: Host에 접근할 수 없습니다.")
```

### 5.3 접속 허용 팝업 타임아웃

```python
APPROVAL_TIMEOUT = 30  # 초

# Host 팝업에서 30초 내 응답 없으면 자동 거부
result = popup.ask_approval(timeout=APPROVAL_TIMEOUT)
if result is None:
    result = False  # 자동 거부
```

---

## 6. 설정 파일 명세

### config.ini 전체 구조

```ini
[network]
; Host가 Listen할 포트
port = 9000
; 소켓 수신 버퍼 크기 (bytes)
buffer_size = 65536
; 재연결 시도 횟수
reconnect_attempts = 3
; 소켓 타임아웃 (초)
socket_timeout = 10

[capture]
; 목표 FPS (1~60)
fps = 20
; JPEG 품질 (1~95)
quality = 60
; 전송 해상도 (0이면 원본 유지)
scale_width = 1280
scale_height = 720

[security]
; 허용 IP 대역 (CIDR 표기)
allowed_ip_range = 192.168.1.0/24
; 접속 시 허용 팝업 표시 여부
require_approval = true
; 세션 자동 종료 시간 (초, 0이면 무제한)
session_timeout = 3600

[logging]
; 로그 파일 저장 경로
log_dir = C:\IHRD\logs
; 로그 레벨 (DEBUG / INFO / WARNING / ERROR)
log_level = INFO
```

### 설정 로드 코드

```python
import configparser
import os

def load_config(path="config.ini"):
    config = configparser.ConfigParser()
    # 실행 파일과 같은 디렉토리에서 config.ini 탐색
    base_dir = os.path.dirname(os.path.abspath(__file__))
    config.read(os.path.join(base_dir, path))
    return config
```

---

## 7. 로깅 설계

### 7.1 로그 형식

```
YYYY-MM-DD HH:MM:SS | LEVEL    | EVENT      | 상세 정보
─────────────────────────────────────────────────────────────────
2026-04-15 09:32:11 | INFO     | CONNECT    | src=192.168.1.42 dst=192.168.1.101 result=ALLOWED
2026-04-15 09:45:03 | INFO     | DISCONNECT | src=192.168.1.42 dst=192.168.1.101 duration=772s
2026-04-15 10:01:55 | WARNING  | CONNECT    | src=192.168.1.88 dst=192.168.1.101 result=DENIED(user)
2026-04-15 10:15:20 | WARNING  | BLOCKED    | src=10.0.0.55 dst=192.168.1.101 reason=IP_NOT_ALLOWED
2026-04-15 10:30:00 | ERROR    | CAPTURE    | Monitor index out of range, skipping frame
```

### 7.2 로거 초기화

```python
import logging
import os
from datetime import datetime

def setup_logger(log_dir: str, level: str = "INFO") -> logging.Logger:
    os.makedirs(log_dir, exist_ok=True)
    log_file = os.path.join(log_dir, f"ihrd_{datetime.now():%Y%m%d}.log")

    logger = logging.getLogger("IHRD")
    logger.setLevel(getattr(logging, level))

    formatter = logging.Formatter(
        "%(asctime)s | %(levelname)-8s | %(message)s",
        datefmt="%Y-%m-%d %H:%M:%S"
    )

    # 파일 핸들러
    fh = logging.FileHandler(log_file, encoding="utf-8")
    fh.setFormatter(formatter)
    logger.addHandler(fh)

    return logger
```

---

## 8. 배포 및 운영 절차

### 8.1 빌드

```bash
# 가상환경 활성화
.venv\Scripts\activate

# Host Agent
pyinstaller --onefile --windowed --name="IHRD-Host" host/main.py

# Viewer App
pyinstaller --onefile --windowed --name="IHRD-Viewer" viewer/main.py
```

### 8.2 대상 PC (Host) 배포

```
1. IHRD-Host.exe 복사
2. config.ini 사내 설정으로 수정 (allowed_ip_range 등)
3. 방화벽 포트 9000 개방 (아래 명령)
4. (선택) 시작 프로그램 등록

# 방화벽 설정 (관리자 PowerShell)
netsh advfirewall firewall add rule `
  name="IHRD Host Agent" dir=in action=allow `
  protocol=TCP localport=9000

# 시작 프로그램 등록 (선택)
# shell:startup 폴더에 IHRD-Host.exe 바로가기 추가
```

### 8.3 제어 PC (Viewer) 배포

```
1. IHRD-Viewer.exe 복사
2. 실행 후 연결 다이얼로그에서 대상 IP 입력
3. 추가 설정 불필요
```

### 8.4 트러블슈팅

| 증상 | 원인 | 해결 |
|------|------|------|
| 연결 거부됨 | Host Agent 미실행 | 대상 PC에서 IHRD-Host.exe 실행 확인 |
| 연결 타임아웃 | 방화벽 차단 | 포트 9000 인바운드 규칙 확인 |
| 화면 안 나옴 | 해상도 설정 오류 | config.ini scale_width/height 확인 |
| 입력 안 됨 | UAC 권한 부족 | IHRD-Host.exe를 관리자 권한으로 실행 |
| 화면 끊김 | 네트워크 혼잡 | config.ini quality 값 낮춤 (40~50) |

---

*문서 버전: v1.0 | 작성일: 2026-04-15 | 역할: 엔지니어링*
