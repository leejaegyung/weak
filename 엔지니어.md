# 주간업무보고 시스템 통합 명세서 (Master Specification)

이 문서는 프로젝트의 기획 요구사항, 개발 표준, 그리고 백엔드/인프라 엔지니어링 설계를 하나로 통합한 마스터 명세서입니다. 모든 개발자와 시스템 운영자는 본 가이드를 최우선으로 준수해야 합니다.

---

## 1. 프로젝트 개요

| 항목 | 내용 |
| :--- | :--- |
| **프로젝트명** | 주간업무보고 시스템 (SE-HUB) |
| **플랫폼** | 웹 (Nginx + FastAPI + Vue 3) |
| **사용 환경** | 사내 LAN / 브라우저 |
| **데이터베이스** | PostgreSQL 17 (표준 준수) |
| **인증 방식** | JWT (8시간) + bcrypt 암호화 |

---

## 2. 핵심 화면 및 기능

### 2.1. 핵심 화면
1. **팀 주간 일정판 (대시보드):** 팀 전체의 금주/차주 일정을 그리드 형태로 관리. 주차 전환 시 차주 내용이 금주로 자동 이동.
2. **개인 주간업무보고:** 전주/금주 업무를 독립적으로 작성. 보고서 작성 시 팀 일정판의 내용을 불러오거나 동시에 기입 가능.

### 2.2. 기능 요구사항 (v1.0 ~ v2.0)
* **F01 로그인:** 역할(Admin/Member) 구분 및 JWT 인증.
* **F02 팀 일정판:** 날짜별 셀 직접 편집 및 자동 주차 전환 로직.
* **F03 주간보고 작성:** 지원/Todo/내부작업/공유 섹션 구성. 개행(`\n`) 처리 및 엔터키 제출 방지 적용.
* **F04 주간보고 열람:** 미작성자 포함 전체 사용자 노출 (정해진 순서대로).
* **F05 인쇄:** 보고서 최적화 레이아웃 제공.
* **F06 사용자 관리:** 관리자 전용 계정 제어.
* **F07 Google Sheets 연동:** 보고서 및 일정판 양방향 동기화.
* **F08 PDF 생성:** 보고서 PDF 자동 변환 및 저장.
* **F09 알림:** 금요일 오전 보고 마감 알림.

---

## 3. 🤖 개발 표준 및 원칙 (Developer Guide)

### 3.1. 기본 원칙
* **문서 기반:** 개발 전 `PRD` 및 본 명세서 검토 필수.
* **언어 규칙:** UI 텍스트, 메시지, 주석은 반드시 **한국어**로 작성.
* **네이밍 컨벤션:**
    * **DB / PHP(Laravel 기준 관례):** `snake_case`
    * **클래스/모델:** `PascalCase`
    * **변수/메소드/JS:** `camelCase`
    * **URL:** `kebab-case`

### 3.2. 프론트엔드 (Vue 3 + Tailwind)
* **Composition API:** `<script setup>` 필수 사용.
* **Inertia.js:** 데이터 바인딩 및 폼 처리는 `useForm` 훅 사용.
* **Styling:** Tailwind CSS 유틸리티 클래스만 사용 (커스텀 CSS 금지).

---

## 4. ⚙️ 백엔드 및 데이터베이스 설계

### 4.1. 아키텍처 (FastAPI)
* **Thin Router:** 요청 수신 및 검증(Pydantic) 역할.
* **Fat Service:** 비즈니스 로직 및 DB 쿼리 전담 (`app/Services` 구조 지향).

### 4.2. 데이터베이스 스키마 (PostgreSQL)

#### 사용자 (users)
* `id`, `username`, `password_hash`, `name`, `position`, `role`, `is_active`
* `sort_order`: 일정판 및 목록 표출 순서 제어용 필드.

#### 주간보고 (reports)
* `week`: 주차 정보 (예: 2026-W17)
* `prev_support`, `curr_support`: 독립된 JSONB 목록 (`[{title, content}]`)
* `todo_items`: JSONB 목록.
* `internal_work`, `shared`: TEXT (개행문자 보존).

#### 일정 (schedules)
* `user_id`, `date`, `content`

---

## 5. 추가 기획 요구사항 상세 구현

1. **일정판-보고서 연동:** 보고서 작성 시 스케줄 데이터를 함께 처리하여 데이터 일관성 유지.
2. **사용자 목록 노출:** `LEFT JOIN`을 사용하여 보고서 미작성 유저도 `sort_order` 순으로 전체 노출.
3. **입력 편의성:** 텍스트 필드 내 엔터키는 개행으로만 작동하며, 폼 제출은 별도 버튼 클릭으로 제한.
4. **주차 자동 전환:** 서버 타임 기준 `target_date`를 계산하여 금주/차주 데이터를 동적으로 응답.

---

## 6. 인프라 및 서비스 연동

* **Nginx 설정:** 포트 80/443 라우팅 (정적 파일 및 API Proxy).
* **Google Drive 연동:** OAuth2 인증을 통한 시트 생성 및 지정 폴더 저장.
  * 파일명 규식: `YYYY.MM.DD SE팀 주간보고 (이름 직급).xlsx`

---
*문서 버전: v2.1 (통합본) | 최종 수정일: 2026-04-27*