# design.md — SE-HUB 주간업무보고 디자인 시스템

> 현재 구현된 UI를 기준으로 정리한 디자인 가이드. 새 화면·컴포넌트를 만들 때 이 문서의 토큰과 패턴을 그대로 따른다.
> 토큰 원본: `app/resources/css/app.css` (`@theme`, `@layer components`)

---

## 1. 디자인 컨셉

**네오브루탈리즘(Neo-Brutalism) + 따뜻한 크림 톤**

- **두꺼운 검정 테두리**(`2px solid #1A1100`)와 **단단한 오프셋 그림자**(`Npx Npx 0 #1A1100`, blur 없음)가 핵심 시그니처
- 라운드는 부드럽게(8~16px)지만 그림자는 날카롭게 → 친근하면서도 또렷한 인상
- 배경은 종이 같은 크림색(`#FFF8EE`), 포인트는 주황·노랑
- 장식 최소화, 정보 위계는 **굵기·색·테두리**로 표현 (그라데이션·반투명 남용 지양)

### 핵심 규칙 (항상 준수)

1. 떠 있는 요소(카드/버튼/뱃지/모달)는 `2px solid #1A1100` 테두리 + 오프셋 그림자
2. 그림자는 **블러 없는 단색** `#1A1100` — `box-shadow: 2px 2px 0 #1A1100` (hover 시 `3px 3px`)
3. hover 인터랙션은 `transform: translate(-1px,-1px)` + 그림자 1px 증가로 "들리는" 느낌
4. 색은 아래 토큰만 사용. 임의 HEX 추가 금지 (상태색은 §3 참고)
5. UI 텍스트·주석은 모두 한국어, 숫자/영문 강조는 Space Grotesk

---

## 2. 컬러 토큰

`app.css @theme` 정의:

| 토큰 | HEX | 용도 |
|------|-----|------|
| `--color-cream` | `#FFF8EE` | 기본 배경(body, 메인 영역) |
| `--color-cream-dark` | `#F5EDDB` | 사이드바·테이블 헤더·푸터 배경 |
| `--color-orange` | `#FD4401` | 주 강조(헤더 바, 1차 버튼, 활성 포인트, 링크) |
| `--color-orange-light` | `#FF6B35` | 주황 hover |
| `--color-yellow` | `#FDCB40` | 보조 강조(선택 상태, 위젯, 아바타 기본) |
| `--color-yellow-light` | `#FFF0A0` | 옅은 강조(기간 뱃지, 오늘 셀 배경) |
| `--color-ink` | `#1A1100` | 본문 텍스트·테두리·그림자 (사실상 검정) |
| `--color-ink-light` | `#4A3F2A` | 보조 텍스트 |
| `--color-ink-muted` | `#9A8F7A` | 캡션·placeholder·비활성 |

**보조 라인/면 색** (코드 상 자주 쓰임): `#E8E0D0`(연한 테두리), `#F0E9DA`(구분선), `#FDFAF5`(아주 옅은 카드면)

### 상태(시맨틱) 색 — 뱃지

| 상태 | 배경 | 글자/테두리 | 클래스 |
|------|------|------------|--------|
| 임시저장 | `#F3F4F6` | `#6B7280` / `#D1D5DB` | `.badge-draft` |
| 제출됨 | `#DBEAFE` | `#1D6FE9` | `.badge-submitted` |
| 검토완료 | `#DCFCE7` | `#16A34A` | `.badge-reviewed` |
| 반려 | `#FEE2E2` | `#DC2626` | `.badge-rejected` |
| 미제출 | `#FFF0A0` | `#92400E` / `#D97706` | `.badge-not-submitted` |

### 일정 상태 태그 (`Schedule` QUICK_TAGS)

| 상태 | 아이콘 | 배경 | 글자 | 테두리 |
|------|--------|------|------|--------|
| 외근 | 🏢 | `#DBEAFE` | `#1D4ED8` | `#93C5FD` |
| 출장 | ✈️ | `#EDE9FE` | `#7C3AED` | `#C4B5FD` |
| 반차 | 🕐 | `#FEF9C3` | `#854D0E` | `#FDE68A` |
| 휴가 | 🌴 | `#DCFCE7` | `#166534` | `#86EFAC` |

### 아바타 색 팔레트

이름/ID 해시로 순환 배정: `#FD4401`, `#16a34a`, `#2563eb`, `#9333ea`, `#d97706`, `#0891b2`, `#dc2626`, `#65a30d`
(사용자가 `avatar_color` 또는 `avatar_image_url`을 가지면 우선 적용)

---

## 3. 타이포그래피

```
--font-family-sans:    'Noto Sans KR', 'Space Grotesk', sans-serif   // 본문 기본
--font-family-display: 'Space Grotesk', 'Noto Sans KR', sans-serif   // 제목·숫자·라벨
```

- **Display(Space Grotesk)**: 페이지 제목, 주차/숫자, 위젯 수치, 대문자 라벨. `letter-spacing:-0.02~-0.03em`
- **본문(Noto Sans KR)**: 한글 가독성 우선

| 역할 | 크기 | 굵기 |
|------|------|------|
| 페이지 제목 (h1) | 26px (모바일 20px) | 700 |
| 섹션/모달 제목 | 15~18px | 800 |
| 본문 | 13~15px | 400~500 |
| 버튼/뱃지/라벨 | 11~13px | 700~800 |
| 캡션·메타 | 10~12px | 400~600, `#9A8F7A` |
| 섹션 소제목(대문자) | 10~12px, `text-transform:uppercase`, `letter-spacing:0.04~0.1em` |

---

## 4. 형태 토큰 (Radius / Shadow / Spacing)

### Border Radius
| 용도 | 값 |
|------|----|
| 카드·모달·큰 컨테이너 | 16px |
| 버튼·인풋·아이콘 박스 | 8~12px |
| 작은 버튼(`btn-sm`)·네비 아이템 | 8~10px |
| 뱃지·진행바·점 | 99px (pill) |
| 아바타 | 50% (원형) |

### Shadow (블러 없는 오프셋, 색은 항상 `#1A1100`)
| 강도 | 값 | 용도 |
|------|----|----|
| 기본 | `2px 2px 0 #1A1100` | 버튼, 활성 네비, 작은 강조 |
| hover/강조 | `3px 3px 0 #1A1100` | hover 상태 |
| 카드·위젯·모달 | `4px 4px 0 #1A1100` | 떠 있는 컨테이너 |
| 툴팁 | `3px 3px 0 rgba(26,17,0,0.28)` | 살짝 옅게 |

### Border
- 주 테두리: `2px solid #1A1100`
- 보조/내부 구분: `1.5px solid #E8E0D0` 또는 `1px solid #F0E9DA`

### Spacing 관습
- 카드 패딩 22px, 모달/바디 패딩 28~32px (모바일 16px)
- 요소 간격 6~16px, 버튼 내부 `9px 18px`(기본) / `5px 12px`(sm)

---

## 5. 컴포넌트 (`@layer components`)

### 버튼
- `.btn-primary` — 주황 배경 + 흰 글자, hover 시 `#FF6B35` + 들림
- `.btn-secondary` — 흰 배경 + 검정 글자, hover 시 `#F5EDDB` + 들림
- `.btn-sm` — 위 클래스에 함께 붙이는 소형 변형 (12px / `5px 12px` / radius 8px)
- 공통: `2px solid #1A1100`, `2px 2px 0` 그림자, 굵기 700, `transition .1s`

### 카드 `.card`
흰 배경 · `2px` 테두리 · radius 16px · `4px 4px 0` 그림자 · 패딩 22px

### 인풋 `.input-field`
크림 배경(`#FFF8EE`) · `2px` 테두리 · radius 8px · focus 시 테두리 `#FD4401` · placeholder `#9A8F7A`
> 폼은 Inertia `useForm` 사용, 하드코딩 검증 금지 (CLAUDE.md 규칙)

### 네비 아이템 `.nav-item`
좌측 정렬 아이콘+라벨 · 투명 테두리 · hover 시 옅은 노랑(`rgba(253,203,64,0.35)`)
활성(`.nav-item-active` 또는 인라인): 페이지별 파스텔 배경 + `2px` 검정 테두리 + `2px 2px 0` 그림자 + 굵기 700
> 페이지별 활성 배경: 일정판 `#FDCB40`, 보고서 `#DBEAFE`, 작성 `#F0FDF4`, 이슈 `#EDE9FE`, 개인설정 `#FFF0F0`, 관리 `#FFF0F3`

### 아바타
원형 · `2px solid #1A1100` · 이미지 없으면 팔레트 배경 + 이름 첫 글자(흰/검정 대비)
크기: 사이드바/헤더 24px, 목록 20px, 카드 34px

### 뱃지
pill 형태(`§2` 상태색) · `1.5px` 테두리 · 11px / 700

### 툴팁 (`v-tooltip` 디렉티브, `utils/tooltip.js`)
검정 배경(`#1A1100`) · 크림 글자(`#FFF8EE`) · `2px` 테두리 · radius 9px · `3px 3px 0 rgba(26,17,0,.28)` 그림자
빈 문자열이면 미표시, 반응형 갱신. 접힘 사이드바·아이콘 버튼의 라벨 대체로 활용.

---

## 6. 레이아웃

```
┌─ header (56px, 주황 #FD4401, 하단 2px 검정 테두리) ─────────────┐
│  로고 + 페이지칩            알림벨 · 유저 · 로그아웃              │
├──────────┬──────────────────────────────────────────────────┤
│ sidebar  │  main (#FFF8EE, 패딩 28×32)                        │
│ 180px    │    slot — 페이지 콘텐츠                             │
│ #F5EDDB  │                                                    │
│ 접힘 64px │                                                    │
│ 하단 위젯 │                                                    │
└──────────┴──────────────────────────────────────────────────┘
```

- **헤더**: 고정 56px, 주황 배경, 우측 알림/유저/로그아웃
- **사이드바**: 180px, 크림다크 배경, 상단 토글로 64px 아이콘 전용 접힘(localStorage 영속), 하단 "이번 주" 노랑 위젯
- **메인**: 크림 배경, 세로 스크롤
- 화면 단위 컨테이너는 `.card`로 감싼다

---

## 7. 반응형 (모바일 ≤ 768px)

- 사이드바 **숨김** → 하단 고정 **모바일 네비바**(`.mobile-bottom-nav`, 62px) 표시. 중앙 작성 버튼은 주황 원형 강조
- 페이지 제목 26→20px, 본문 패딩 16px, 하단 네비 공간 80px 확보
- 다열 폼 그리드는 1열로 접힘(7열 달력 등 고정 그리드는 제외)
- 가로 넘침 방지: 아바타/칩 목록은 `flex-wrap:wrap`으로 줄바꿈 (가로 스크롤로 잘리지 않게)
- 매우 좁은 화면(≤380px): 하단 네비 버튼·작성 버튼 추가 축소

---

## 8. 모션

- 인터랙션 트랜지션 `0.1~0.18s`, 짧고 또렷하게
- hover: `translate(-1px,-1px)` + 그림자 +1px (눌림/들림)
- active(모바일 작성 버튼): `translate(1px,1px)` + 그림자 -1px
- 드롭다운/서브메뉴: `opacity + translateY(-4~6px)` 페이드, `0.18s ease`

---

## 9. 메일 템플릿 스타일 (`mail/weekly_report.blade.php`)

웹과 동일한 네오브루탈리즘을 인라인/스타일 태그로 재현:
- 본문 래퍼 max 600px · 흰 배경 · `2px` 테두리 · radius 16px · `4px 4px 0` 그림자
- 주황 헤더 + 노랑 아이콘 박스, 기간은 `#FFF0A0` 뱃지
- 보고서 목록은 **이름 링크 행**(아바타 + 주황 밑줄 이름 + "바로가기 →")
- 이메일 클라이언트 호환을 위해 토큰 값을 직접 HEX로 작성 (CSS 변수 미사용)

---

## 10. 체크리스트 (새 UI 작성 시)

- [ ] 색은 §2 토큰만 사용했는가
- [ ] 떠 있는 요소에 `2px #1A1100` 테두리 + 블러 없는 오프셋 그림자를 줬는가
- [ ] 버튼/카드/인풋은 기존 컴포넌트 클래스를 재사용했는가 (`<style>` 태그 금지, Tailwind 유틸/공통 클래스)
- [ ] 제목·숫자는 Space Grotesk, 본문은 Noto Sans KR인가
- [ ] hover/active 모션이 기존 패턴과 일치하는가
- [ ] 모바일 ≤768px에서 깨지지 않는가 (줄바꿈·1열·하단 네비)
- [ ] UI 텍스트가 한국어인가
